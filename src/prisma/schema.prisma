generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = "postgresql://akepoguvivek14:FP0rYczKCwq7@ep-dark-rain-45291804.us-east-2.aws.neon.tech/useraddress?sslmode=require"
}

model User {
    userId  String   @id @db.VarChar(225)
    address Address?
}

model Address {
    addressId  String @id @default(cuid())
    userId     String @unique
    user       User   @relation(fields: [userId], references: [userId])
    number     String
    street     String
    city       String
    postalCode String
    country    String
}
